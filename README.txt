This is a movie watchlist that allows you to add movies, mark them as watched,
and leave a review and rating.

Start the program by entering "node app.js" in the terminal.
Then enter "http://localhost:3000/" into the address bar in your internet browser.
Enter "npx kill-port 3000" into terminal to terminate the program.
